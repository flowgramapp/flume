{
  "name": "docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "docusaurus start",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && cd .. && npm run build && cd docs && docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy"
  },
  "dependencies": {
    "@docusaurus/core": "^2.0.0-beta.14",
    "@docusaurus/preset-classic": "^2.0.0-beta.14",
    "clsx": "^1.1.1",
    "flume": "git+ssh://git@github.com/flowgramapp/flume.git",
    "lodash": "^4.17.19",
    "react-portal": "^4.2.1",
    "react-spring": "^8.0.27"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
